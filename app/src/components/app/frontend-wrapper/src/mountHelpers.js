/**
 * Frontend mount helpers
 *
 * This file exists to satisfy the import in:
 * src/components/app/frontend-wrapper/index.vue
 *
 * The current build may not use it directly, but it must exist for Vite/Rollup resolution.
 */
export const mountHelpers = function mountHelpers() {
  return {};
};
